programa {
    funcao inicio () {
        inteiro contador + 0 
        enquanto(contador < 10){
            // Aqui vai o código
            escreva("Você já deu", contador, "voltas")
            //0
            contador = contador +1
            // 1 -> 1
            // 2 -> 1
            // 3 -> 1
            // 4
        }    
    }
}